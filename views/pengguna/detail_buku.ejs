<!DOCTYPE html>
<html lang="id">
<head>
    <%- include('../partials/header/__headerPengguna.ejs', { title: 'Detail Buku' }) %>
</head>
<body>
    <div class="container-fluid page-body-wrapper flex flex-grow">
        <div class="container mb-5">
            <a href="javascript:history.back()" class="btn-back mb-3">
                <i class="bi bi-arrow-left"></i>
                Kembali
            </a>
            
            <div class="row">
                <div class="col-md-4 text-center mb-4">
                    <a href="#" class="book-cover-trigger" id="book-cover-link"> 
                        <img src="/images/buku/<%= buku.foto_cover %>" 
                             alt="Cover Buku" 
                             class="detail-cover-img" 
                             id="book-cover">
                    </a>
                </div>

                <div id="myModal" class="modal">
                    <span class="close" title="Tutup Modal">&times;</span>
                    <img class="modal-content" id="img01">
                    <div id="caption"></div>
                </div>
                
                <div class="col-md-8">
                    <h1 class="detail-header" id="book-title"><%= buku.judul %></h1>
                    <div class="mb-4">
                        <div class="detail-info-item">
                            <span class="detail-info-label">Pengarang</span>
                            <span class="detail-info-value" id="book-author"><%= buku.pengarang || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">ISBN / ISSN</span>
                            <span class="detail-info-value" id="book-isbn"><%= buku.isbn_issn || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">No. Klasifikasi</span>
                            <span class="detail-info-value" id="book-classification"><%= buku.no_klasifikasi || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">Bahasa</span>
                            <span class="detail-info-value" id="book-language"><%= buku.bahasa || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">Jumlah Halaman</span>
                            <span class="detail-info-value" id="book-pages"><%= buku.jumlah_halaman || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">Tahun Terbit</span>
                            <span class="detail-info-value" id="book-year"><%= buku.tahun_terbit || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">Tempat Terbit</span>
                            <span class="detail-info-value" id="book-publication-place"><%= buku.tempat_terbit || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">Penerbit</span>
                            <span class="detail-info-value" id="book-publisher"><%= buku.penerbit || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">Kategori</span>
                            <span class="detail-info-value" id="book-category"><%= buku.kategori || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">Lokasi</span>
                            <span class="detail-info-value" id="book-location"><%= buku.lokasi || '-' %></span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">Ketersediaan</span>
                            <span class="detail-info-value" id="book-availability">
                                <span class="availability-badge <%= buku.ketersediaan === 'Tersedia' ? 'availability-available' : 'availability-unavailable' %>">
                                    <%= buku.ketersediaan || 'Tidak Diketahui' %>
                                </span>
                            </span>
                        </div>
                        <div class="detail-info-item">
                            <span class="detail-info-label">Sinopsis</span>
                            <span class="detail-info-value" id="book-synopsis"><%= buku.sinopsis || 'Sinopsis tidak tersedia.' %></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <%- include('../partials/footer/__footPengguna.ejs') %>
    <%- include('../partials/script/__scriptPengguna.ejs') %>
</body>
</html>