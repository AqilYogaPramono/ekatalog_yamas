<!DOCTYPE html>
<html lang="id">
<head>
    <%- include('../partials/header/__headerPengguna.ejs', { title: 'Katalog Perpustakaan' }) %>
</head>
<body>
    <section class="hero-section text-center d-flex align-items-center justify-content-center">
        <div class="container">
            <h1 class="hero-title mb-4">Temukan Koleksi Pustaka Kami</h1>
            <p class="hero-subtitle mb-5">Jelajahi ribuan buku dan majalah yang tersedia.</p>

            <form id="search-form" class="search-form" method="POST" action="/">
                <div class="input-group search-container">
                    <span class="input-group-text search-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16"
                            fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                            <path
                                d="M11.742 10.344a6.5 6.5 0 1 0-1.397 
                                1.398h-.001q.044.06.085.122l3.023 
                                3.023a.75.75 0 0 0 1.06-1.06l-3.023-3.023
                                q-.062-.041-.122-.085zm-5.442 
                                1.597a5.25 5.25 0 1 1 0-10.5 
                                5.25 5.25 0 0 1 0 10.5"/>
                        </svg>
                    </span>
                    <input type="text" id="search-input" name="keyword" class="form-control" placeholder="Cari buku atau majalah" value="<%= typeof keyword !== 'undefined' ? keyword : '' %>">
                    
                    <button type="button" id="clear-search" class="btn btn-link px-2 d-none">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="currentColor"
                            class="bi bi-x-circle text-secondary" viewBox="0 0 16 16">
                            <path d="M8 15A7 7 0 1 0 8 1a7 7 0 
                                0 0 0 14zm3.354-9.646a.5.5 0 
                                0 1 0 .708L8.707 8l2.647 2.646a.5.5 
                                0 0 1-.708.708L8 8.707l-2.646 
                                2.647a.5.5 0 0 1-.708-.708L7.293 
                                8 4.646 5.354a.5.5 0 1 1 
                                .708-.708L8 7.293l2.646-2.647a.5.5 
                                0 0 1 .708 0z"/>
                        </svg>
                    </button>
                    <button class="btn btn-primary search-button" type="submit">Cari</button>
                </div>
            </form>
        </div>
    </section>

    <div class="container mt-5 mb-5">
        <div id="search-results-section">
            <div class="section-title-wrapper d-flex justify-content-between align-items-center mb-4">
                <h3 class="section-title">Hasil Pencarian</h3>
                <% if (typeof result !== 'undefined' && result.length > 0) { %>
                    <span class="result-count"><%= result.length %> hasil ditemukan</span>
                <% } %>
            </div>
            
            <div class="search-content">
                <% if (typeof result !== 'undefined') { %>
                    <% if (result.length > 0) { %>
                        <div class="row row-cols-2 row-cols-md-4 g-4" id="search-results-list">
                            <% result.forEach(item => { %>
                                <div class="col mb-3">
                                    <div class="card h-100 modern-card">
                                        <div class="card-img-wrapper">
                                            <img src="/images/<%= item.tipe === 'Buku' ? 'buku' : 'majalah' %>/<%= item['foto-cover'] %>" 
                                                 class="<%= item.tipe === 'Buku' ? 'book-cover' : 'magazine-cover' %>" 
                                                 alt="Cover <%= item.tipe %>"
                                                 loading="lazy">
                                            <div class="tipe-badge"><%= item.tipe %></div>
                                        </div>
                                        <div class="card-body">
                                            <h5 class="card-title"><%= item.judul %></h5>
                                            <% if (item.tipe === 'Majalah') { %>
                                                <p class="card-text text-muted small">Edisi: <%= item.edisi %></p>
                                                <p class="card-text text-muted small">No. Klasifikasi: <%= item.no_klasifikasi %></p>
                                                <p class="card-text text-muted small">Lokasi: <%= item.lokasi %></p>
                                            <% } else { %>
                                                <p class="card-text text-muted">No. Klasifikasi: <%= item.no_klasifikasi %></p>
                                                <p class="card-text text-muted small">Lokasi: <%= item.lokasi %></p>
                                            <% } %>
                                            <a href="/detail/<%= item.tipe %>/<%= item.id %>" class="stretched-link"></a>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    <% } else { %>
                        <div class="no-results-state">
                            <div class="no-results-icon">
                                <svg xmlns="http://www.w3.org/2000/svg" width="80" height="80" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                                    <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001q.044.06.085.122l3.023 3.023a.75.75 0 0 0 1.06-1.06l-3.023-3.023q-.062-.041-.122-.085zm-5.442 1.597a5.25 5.25 0 1 1 0-10.5 5.25 5.25 0 0 1 0 10.5"/>
                                </svg>
                            </div>
                            <h4 class="no-results-title">Tidak ada hasil ditemukan</h4>
                            <p class="no-results-message">Tidak ada hasil untuk kata kunci "<%= keyword %>"</p>
                            <p class="no-results-suggestion">Silakan coba dengan kata kunci yang berbeda atau periksa ejaan kata kunci Anda</p>
                        </div>
                    <% } %>
                <% } else { %>
                    <div class="empty-state">
                        <div class="empty-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="80" height="80" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                                <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001q.044.06.085.122l3.023 3.023a.75.75 0 0 0 1.06-1.06l-3.023-3.023q-.062-.041-.122-.085zm-5.442 1.597a5.25 5.25 0 1 1 0-10.5 5.25 5.25 0 0 1 0 10.5"/>
                            </svg>
                        </div>
                        <h4 class="empty-title">Mulai Pencarian Anda</h4>
                        <p class="empty-message">Gunakan kolom pencarian di atas untuk menemukan buku atau majalah</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
    
    <%- include('../partials/footer/__footPengguna.ejs') %>
    <%- include('../partials/script/__scriptPengguna.ejs') %>
</body>
</html>