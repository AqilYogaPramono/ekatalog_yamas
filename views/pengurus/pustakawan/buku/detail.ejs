<!DOCTYPE html>
<html lang="id">

<head>
    <%- include('../../../partials/__head.ejs', { title: 'Detail Buku' }) %>
    <style>
        :root {
            --primary-font: 'Poppins', sans-serif;
            --bg-color: #f8f9fa;
            --card-bg: white;
            --text-primary: #333;
            --text-secondary: #6c757d;
            --highlight-color: #495057;
            --btn-edit: #ffc107;
            --btn-edit-hover: #e0a800;
            --btn-delete: #dc3545;
            --btn-delete-hover: #c82333;
            --shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
        }

        body {
            font-family: var(--primary-font);
            background-color: var(--bg-color);
        }

        .container-fluid {
            background-color: white;
        }

        .container-main {
            max-width: 900px;
            margin: 2rem auto;
            padding: 0 15px;
        }

        .card {
            background-color: var(--card-bg);
            border-radius: 12px;
            box-shadow: var(--shadow);
            padding: 2.5rem;
        }

        .book-header {
            display: flex;
            align-items: flex-start;
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .book-cover {
            flex-shrink: 0;
            width: 200px;
            height: 300px;
            object-fit: cover;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .book-info {
            flex-grow: 1;
        }

        .book-title {
            font-size: 2rem;
            font-weight: 700;
            color: var(--text-primary);
            margin: 0;
            line-height: 1.2;
        }

        .book-author {
            font-size: 1.2rem;
            font-weight: 500;
            color: var(--text-secondary);
            margin-top: 5px;
            margin-bottom: 1.5rem;
        }

        .book-metadata {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
        }

        .metadata-item {
            font-size: 0.95rem;
            line-height: 1.5;
        }

        .metadata-label {
            font-weight: 600;
            color: var(--highlight-color);
            display: block;
        }

        .metadata-value {
            color: var(--text-secondary);
        }

        .sinopsis {
            margin-top: 2rem;
        }

        .sinopsis-title {
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
        }

        .sinopsis-text {
            font-size: 0.95rem;
            color: var(--text-secondary);
            line-height: 1.6;
        }

        .button-group {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
            margin-top: 2rem;
        }

        .btn-edit,
        .btn-delete {
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: 600;
            border: none;
            cursor: pointer;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: background-color 0.2s ease, transform 0.2s ease;
        }

        .btn-edit {
            background-color: var(--btn-edit);
            color: white;
        }

        .btn-edit:hover {
            background-color: var(--btn-edit-hover);
            transform: translateY(-2px);
        }

        .btn-delete {
            background-color: var(--btn-delete);
            color: white;
        }

        .btn-delete:hover {
            background-color: var(--btn-delete-hover);
            transform: translateY(-2px);
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.4);
            animation: fadeIn 0.3s;
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            background-color: #fff;
            margin: auto;
            padding: 30px;
            border-radius: 12px;
            width: 90%;
            max-width: 400px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            text-align: center;
            animation: slideIn 0.4s;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 15px;
        }

        .modal-icon {
            font-size: 3rem;
        }

        .modal-content h3 {
            margin: 0;
            color: #333;
            font-weight: 600;
        }

        .modal-content p {
            margin: 0;
            color: #6c757d;
        }

        .modal-buttons {
            display: flex;
            justify-content: center;
            gap: 10px;
            width: 100%;
        }

        .modal-confirm-btn,
        .modal-cancel-btn {
            padding: 10px 20px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            flex: 1;
            border: none;
        }

        .modal-confirm-btn {
            background-color: var(--btn-delete);
            color: white;
        }

        .modal-confirm-btn:hover {
            background-color: var(--btn-delete-hover);
        }
        
        .modal-cancel-btn {
            background-color: #6c757d;
            color: white;
        }

        .modal-cancel-btn:hover {
            background-color: #5a6268;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        @keyframes slideIn {
            from {
                transform: translateY(-50px);
                opacity: 0;
            }

            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @media (max-width: 768px) {
            .book-header {
                flex-direction: column;
                align-items: center;
                text-align: center;
            }

            .book-cover {
                width: 150px;
                height: 225px;
            }

            .book-title {
                font-size: 1.5rem;
            }

            .book-author {
                font-size: 1rem;
            }

            .book-metadata {
                grid-template-columns: 1fr;
            }

            .button-group {
                flex-direction: column;
                align-items: center;
            }

            .btn-edit,
            .btn-delete {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>

<body>
    <div class="container-scroller">
        <div class="container-fluid page-body-wrapper flex ">
            <%- include('../../../partials/__nav.ejs') %>
            <main class="container-main">
                <div class="card">
                    <header class="book-header">
                        <img src="<%= buku.cover_image_url || 'https://via.placeholder.com/200x300.png?text=No+Cover' %>" alt="Sampul Buku" class="book-cover">
                        <div class="book-info">
                            <h1 class="book-title">
                                <%= buku.judul || 'Judul Tidak Tersedia' %>
                            </h1>
                            <p class="book-author">
                                <%= buku.pengarang || 'Pengarang Tidak Tersedia' %>
                            </p>
                            <dl class="book-metadata">
                                <div class="metadata-item">
                                    <dt class="metadata-label">ISBN/ISSN:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.isbn_issn || '-' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">No. Klasifikasi:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.no_klasifikasi || '-' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Bahasa:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.bahasa || '-' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Penerbit:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.penerbit || '-' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Tahun Terbit:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.tahun_terbit || '-' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Jumlah Halaman:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.jumlah_halaman || '-' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Kategori:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.kategori || '-' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Ketersediaan:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.ketersediaan || '-' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Lokasi:</dt>
                                    <dd class="metadata-value">
                                        <%= (buku.lokasi && buku.lokasi.lantai) ? `Lantai: ${buku.lokasi.lantai}, Ruangan: ${buku.lokasi.ruangan}, Rak: ${buku.lokasi.rak}` : 'Tidak Tersedia' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Dibuat Pada:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.createdAt ? new Date(buku.createdAt).toLocaleString('id-ID', { dateStyle: 'long', timeStyle: 'short' }) : 'Tidak Tersedia' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Dibuat Oleh:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.createdBy || 'Tidak Diketahui' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Diedit Pada:</dt>
                                    <dd class="metadata-value">
                                        <%= (buku.updatedAt && buku.updatedAt.toString() !== buku.createdAt.toString()) ? new Date(buku.updatedAt).toLocaleString('id-ID', { dateStyle: 'long', timeStyle: 'short' }) : 'Belum Pernah Diubah' %>
                                    </dd>
                                </div>
                                <div class="metadata-item">
                                    <dt class="metadata-label">Diedit Oleh:</dt>
                                    <dd class="metadata-value">
                                        <%= buku.editedBy || 'Belum Pernah Diedit' %>
                                    </dd>
                                </div>
                            </dl>
                        </div>
                    </header>

                    <section class="sinopsis">
                        <h5 class="sinopsis-title">Sinopsis</h5>
                        <p class="sinopsis-text">
                            <%= buku.sinopsis || 'Sinopsis tidak tersedia.' %>
                        </p>
                    </section>

                    <div class="button-group">
                        <a href="/buku/edit/<%= buku.id %>" class="btn-edit">
                            <i class="bi bi-pencil-fill"></i> Edit
                        </a>
                        <button class="btn-delete" id="deleteBtn">
                            <i class="bi bi-trash-fill"></i> Hapus
                        </button>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <div id="deleteConfirmationModal" class="modal">
        <div class="modal-content">
            <i class="bi bi-exclamation-triangle-fill modal-icon" style="color: #ffc107;"></i>
            <h3>Konfirmasi Hapus</h3>
            <p>Apakah Anda yakin ingin menghapus buku ini?</p>
            <div class="modal-buttons">
                <button type="button" class="modal-confirm-btn" id="yesDeleteBtn">Ya, Hapus</button>
                <button type="button" class="modal-cancel-btn" id="noDeleteBtn">Batal</button>
            </div>
        </div>
    </div>

    <%- include('../../../partials/__foot.ejs') %>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const deleteBtn = document.getElementById('deleteBtn');
            const deleteConfirmationModal = document.getElementById('deleteConfirmationModal');
            const yesDeleteBtn = document.getElementById('yesDeleteBtn');
            const noDeleteBtn = document.getElementById('noDeleteBtn');
            
            const bukuId = "<%= buku.id %>";

            deleteBtn.addEventListener('click', () => {
                deleteConfirmationModal.style.display = 'flex';
            });

            yesDeleteBtn.addEventListener('click', () => {
                window.location.href = `/buku/delete/${bukuId}`;
            });

            noDeleteBtn.addEventListener('click', () => {
                deleteConfirmationModal.style.display = 'none';
            });

            window.addEventListener('click', function(event) {
                if (event.target === deleteConfirmationModal) {
                    deleteConfirmationModal.style.display = 'none';
                }
            });
        });
    </script>
</body>

</html>