<%- include('../../partials1/header/__header.ejs', { title: 'Daftar Majalah Dihapus' }) %>
<%- include('../../partials1/nav-side/__nav-saideAdmin.ejs') %>

<!-- Loading Spinner -->
<div id="loadingSpinner" style="display: none;">
  <div class="spinner"></div>
  <p>Uploading dan memproses file... Mohon tunggu.</p>
</div>

<style>
  #loadingSpinner {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background: rgba(255, 255, 255, 0.85);
    z-index: 9999;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    font-family: sans-serif;
  }

  .spinner {
    width: 48px;
    height: 48px;
    border: 5px solid #f3f3f3;
    border-top: 5px solid #3498db;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin-bottom: 15px;
  }

  @keyframes spin {
    0% { transform: rotate(0); }
    100% { transform: rotate(360deg); }
  }
</style>

<div class="col-lg-12 grid-margin stretch-card">
  <div class="card">
    <div class="card-body">

      <h4 class="card-title mb-4">Daftar Majalah Dihapus</h4>

      <% if (majalah.length === 0) { %>
        <p>Tidak ada data majalah.</p>
      <% } else { %>
        <div class="list-group">
          <% majalah.forEach((item) => { %>
            <div class="list-group-item mb-3 border rounded">
              <div class="d-flex justify-content-between align-items-center">
                <div>
                  <strong><%= item.judul %></strong><br>
                  <small>Edisi: <%= item.edisi || '-' %> | Bahasa: <%= item.bahasa || '-' %></small>
                </div>
                <div class="d-flex align-items-center gap-2">
                  <button class="btn btn-sm btn-outline-secondary" type="button" data-toggle="collapse" data-target="#collapse<%= item.id %>">
                    Detail <i class="mdi mdi-chevron-down"></i>
                  </button>

                  <!-- Tampilkan (Edit Status) Modal Trigger -->
                  <button class="btn btn-sm btn-success" data-toggle="modal" data-target="#modalTampilkan<%= item.id %>">
                    <i class="mdi mdi-eye"></i> Tampilkan
                  </button>

                  <!-- Delete Modal Trigger -->
                  <button class="btn btn-sm btn-danger" data-toggle="modal" data-target="#modalDelete<%= item.id %>">
                    <i class="mdi mdi-delete"></i> Hapus
                  </button>
                </div>
              </div>

              <!-- Detail Collapse -->
              <div class="collapse mt-3" id="collapse<%= item.id %>">
                <div class="border-top pt-3">
                  <div class="mb-3">
                    <img src="/images/majalah/<%= item.foto_cover %>" alt="Cover" style="width: 120px; height: auto; object-fit: cover;">
                  </div>
                  <p><strong>No Klasifikasi:</strong> <%= item.no_klasifikasi || '-' %></p>
                  <p><strong>Tahun Terbit:</strong> <%= item.tahun_terbit || '-' %></p>
                  <p><strong>Tempat Terbit:</strong> <%= item.tempat_terbit || '-' %></p>
                  <p><strong>Penerbit:</strong> <%= item.penerbit || '-' %></p>
                  <p><strong>Lokasi:</strong> <%= item.lokasi_majalah || '-' %></p>
                  <p><strong>Ketersediaan:</strong> <%= item.ketersediaan %></p>
                  <p><strong>Sinopsis:</strong> <%= item.sinopsis || '-' %></p>
                  <p><strong>Dihapus Oleh:</strong> <%= item.dihapus_oleh %></p>
                  <p><strong>Dihapus Pada:</strong> 
                    <% if (item.dihapus_pada) { %>
                      <%= new Date(item.dihapus_pada).toLocaleString() %>
                    <% } else { %> - <% } %>
                  </p>
                </div>
              </div>

              <!-- Modal: Tampilkan -->
              <div class="modal fade" id="modalTampilkan<%= item.id %>" tabindex="-1" role="dialog" aria-labelledby="modalTampilkanLabel<%= item.id %>" aria-hidden="true">
                <div class="modal-dialog" role="document">
                  <form action="/admin/majalah/edit/<%= item.id %>" method="POST">
                    <div class="modal-content">
                      <div class="modal-header">
                        <h5 class="modal-title" id="modalTampilkanLabel<%= item.id %>">Tampilkan Majalah</h5>
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                      </div>
                      <div class="modal-body">
                        Apakah Anda yakin ingin menampilkan kembali majalah "<%= item.judul %>"?
                        <input type="hidden" name="status_data" value="Tampil">
                      </div>
                      <div class="modal-footer text-right">
                        <button type="button" class="btn btn-light" data-dismiss="modal">Batal</button>
                        <button type="submit" class="btn btn-success">Tampilkan</button>
                      </div>
                    </div>
                  </form>
                </div>
              </div>

              <!-- Modal: Delete -->
              <div class="modal fade" id="modalDelete<%= item.id %>" tabindex="-1" role="dialog" aria-labelledby="modalDeleteLabel<%= item.id %>" aria-hidden="true">
                <div class="modal-dialog" role="document">
                  <form action="/admin/majalah/delete/<%= item.id %>" method="POST">
                    <div class="modal-content">
                      <div class="modal-header">
                        <h5 class="modal-title" id="modalDeleteLabel<%= item.id %>">Konfirmasi Hapus</h5>
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                      </div>
                      <div class="modal-body">
                        Apakah Anda yakin ingin menghapus majalah "<%= item.judul %>" secara permanen?
                      </div>
                      <div class="modal-footer text-right">
                        <button type="button" class="btn btn-light" data-dismiss="modal">Batal</button>
                        <button type="submit" class="btn btn-danger">Hapus</button>
                      </div>
                    </div>
                  </form>
                </div>
              </div>

            </div>
          <% }) %>
        </div>
      <% } %>

    </div>
  </div>
</div>

<%- include('../../../partialsAdmin/__script.ejs') %>

<script>
  const uploadForm = document.getElementById('formUploadBatch')
  const spinner = document.getElementById('loadingSpinner')

  if (uploadForm) {
    uploadForm.addEventListener('submit', function () {
      spinner.style.display = 'flex'
    })
  }
</script>
