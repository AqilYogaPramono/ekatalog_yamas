<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/__head.ejs') %>
</head>
<body>
    <div class="container-scroller">
        <div class="container-fluid page-body-wrapper">   
            <%- include('../partials/__nav.ejs') %>         
            <div class="main-panel">
                <div class="content-wrapper">
                    <!-- <div class="row">
                        <div class="col-lg-12 grid-margin stretch-card">
                            <div class="card">
                                <div class="card-body">
                                    <h4 class="card-title">Daftar Buku</h4>
                                    <div class="table-responsive">
                                        <table class="table table-striped">
                                            <thead class="bg-primary text-white">
                                                <tr>
                                                    <th>Judul</th>
                                                    <th>Penulis</th>
                                                    <th>Penerbit</th>
                                                    <th>Tahun</th>
                                                    <th>Klasifikasi</th>
                                                    <th>Status</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% if (buku && buku.length > 0) { %>
                                                    <% buku.forEach(function(item) { %>
                                                        <tr>
                                                            <td><%= item.judul %></td>
                                                            <td><%= item.penulis %></td>
                                                            <td><%= item.penerbit %></td>
                                                            <td><%= item.tahun %></td>
                                                            <td><%= item.klasifikasi %></td>
                                                            <td><label class="badge <%= item.status === 'Tersedia' ? 'badge-success' : 'badge-danger' %>"><%= item.status %></label></td>
                                                        </tr>
                                                    <% }); %>
                                                <% } else { %>
                                                    <tr>
                                                        <td colspan="6">Belum ada data buku.</td>
                                                    </tr>
                                                <% } %>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div> -->

                    <div class="row mt-4">
                        <div class="col-lg-12 grid-margin stretch-card mt-4">
                            <div class="card">
                                <div class="card-body">
                                    <h4 class="card-title">Daftar Majalah</h4>
                                    <div class="table-responsive">
                                        <table class="table table-striped">
                                            <thead class="bg-primary text-white">
                                                <tr>
                                                    <th>Judul</th>
                                                    <th>Edisi</th>
                                                    <th>Penerbit</th>
                                                    <th>Tahun</th>
                                                    <th>Klasifikasi</th>
                                                    <th>Status</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% if (majalah && majalah.length > 0) { %>
                                                    <% majalah.forEach(function(item) { %>
                                                        <tr>
                                                            <td><%= item.judul %></td>
                                                            <td><%= item.edisi %></td>
                                                            <td><%= item.penerbit %></td>
                                                            <td><%= item.tahun %></td>
                                                            <td><%= item.klasifikasi %></td>
                                                            <td><label class="badge <%= item.status === 'Tersedia' ? 'badge-success' : 'badge-danger' %>"><%= item.status %></label></td>
                                                        </tr>
                                                    <% }); %>
                                                <% } else { %>
                                                    <tr>
                                                        <td colspan="6">Belum ada data majalah.</td>
                                                    </tr>
                                                <% } %>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                </div> </div> </div> <%- include('../partials/__foot.ejs') %> 
    </body>
</html>